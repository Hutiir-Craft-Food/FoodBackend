spring:
  config:
    import: optional:file:.env.local[.properties]

  jpa:
    generate-ddl: false
    properties:
      hibernate:
        show-sql: true
        format_sql: true


logging:
  level:
    root: INFO
    com:
      khutircraftubackend: DEBUG