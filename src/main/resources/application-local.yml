spring:
  config:
    import: optional:file:.env.local[.properties]

  jpa:
    generate-ddl: false
    properties:
      hibernate:
        show-sql: true
        format_sql: true

jwt:
  expiration: 60_000 # 1 minute

logging:
  level:
    root: INFO
    com:
      khutircraftubackend: DEBUG