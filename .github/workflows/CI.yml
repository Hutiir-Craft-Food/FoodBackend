name: FoodMarket CI

on:
  pull_request:
    types: ['synchronize', 'opened', 'ready_for_review']
    branches: ['main']
    paths-ignore: ['.gitignore', '*.md']

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:latest
        env:
          POSTGRES_DB: khutir_craft
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    env:
      FLYWAY_EMAIL: ${{ secrets.FLYWAY_EMAIL }}
      FLYWAY_ROLE: ${{ secrets.FLYWAY_ROLE }}
      JWT_SECRET: dk59GJKDslfl45kkdIUOWNNXVG495fjkdLSLLEWIkvmidorfpwa20658764UEmcskRIIKORcfd
      SPRING_DATASOURCE_URL: jdbc:postgresql://localhost:5432/khutir_craft
      SPRING_DATASOURCE_USERNAME: postgres
      SPRING_DATASOURCE_PASSWORD: postgres

    steps:
      - name: Wait for PostgreSQL to be ready
        run: |
          until pg_isready -h localhost -p 5432 -U postgres; do
            echo "Waiting for PostgreSQL to be ready..."
            sleep 1
          done

      - uses: actions/checkout@v3

      - name: Set up JDK 17
        uses: actions/setup-java@v3
        with:
          java-version: '17'
          distribution: 'adopt'
          architecture: x64
          cache: 'maven'

      - name: Build with Maven
        run: mvn clean install

      - name: Finalize Build
        run: echo "Build and tests completed successfully!"
